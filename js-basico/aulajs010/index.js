const nome = 'Luiz'; // String 
const nome1 = "Luiz"; // String
const nome2 = `Luiz`; // String
const num1 = 10; //Number
const num2 = 10.52 //Number
let nomeAluno; // Undefined -> não aponta pra local nenhum na memoria 
const sobrenomeAluno = null; //Nulo -> Não aponta para local nenhum na memoria mas isso porque o programagor quer ou precisa
const aprovado = true; // Boolean = true, false (lógico) sempre para mudar o fluxo da aplicação

console.log(typeof aprovado )
